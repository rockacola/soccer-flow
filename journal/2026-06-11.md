# 2026-06-11

## What I worked on

Applied `typeScale` and `spacing` constants across all 28 remaining StyleSheet files — screens, modals, and components.

## Decisions made

Off-scale values (fontSize 11, 18, 24, 30, 32; spacing 6, 10, 14, 32, 40) left as literals; only values with exact scale matches were replaced.

## What I learned

Nothing new — straightforward mechanical application of the scale established last session.

## Problems / friction

None. TypeScript and all 120 tests passed clean on the first run.

## Next step

Design or feature work — the design system foundation (palette, theme, typeScale, spacing) is now fully applied.

---

## What I worked on

Removed swipe-to-delete from all three list contexts (team list, player list, match list) and the activity log in the live match screen.

## Decisions made

Replaced swipe with an always-visible "Delete" text button on each row. This is the baseline affordance — future platforms or feature flags can layer richer gestures on top. `DeleteAction.tsx` deleted (became dead code). `GestureHandlerRootView` removed from `App.tsx` since nothing uses gesture-handler anymore.

## Next step

Continue incremental UI simplification — next candidate is team colour affordance.

---

## What I worked on

Audited all `useEffect` calls. Eliminated 6 form-reset effects across modal components by replacing them with `key` prop remounts and direct prop initialisation.

## Decisions made

Form-reset effects (`if (visible) { setState(...) }`) replaced with `key={editingActivity?.id ?? 'new'}` / `key={String(visible)}` at call sites — React remounts the component with fresh initial state instead of syncing state after render. `TimerAdjustModal` refs also removed; lazy `useState` initialisers handle the computed initial value directly. Kept: timer interval in `useMatchLiveScreen` (drives re-renders for the live clock) and navigation effects (React Navigation's documented pattern for `setOptions` and blur listeners).

---

## What I worked on

Moved delete actions off list rows and onto detail/edit screens. TeamDetailScreen gets "Delete Team" footer button, EditPlayerModal gets "Delete Player" button, MatchDetailScreen gets "Delete Match" button. All use `Alert.alert` with a destructive confirmation. Activity log delete in MatchLiveScreen stays inline (no detail view to navigate to).

## Decisions made

Outlined-destructive button style (border + red text, no fill) chosen over solid red — less alarming when scanning a screen, still clearly destructive on tap. Alert confirmation is the iOS-standard guard against accidental deletes.
