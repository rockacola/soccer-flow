# 2026-06-09

## What I worked on

- Team detail header: tappable team name with colour dot opens `EditTeamModal` (new) for editing name and colour
- Added `updateTeam` to `teamsService`
- Match setup screen: header now shows "New Match · ● Team Name" with a native back button (`HeaderBackButton`) that returns to the correct team detail screen; removed "My Team" body section; "2 periods" shown as a plain caption (not a card); Start Match button pinned to bottom
- Fixed a navigation bug where finishing a match pushed a duplicate `MatchesList` onto the stack — replaced `navigate('MatchesList')` with `popToTop()`
- Added swipe-to-delete to match listing (new `MatchRow` component, `deletePastMatch` service/store action); both team and match lists now close the open swipeable when another item is swiped or the screen loses focus

## Decisions made

- `HeaderBackButton` from `@react-navigation/elements` used explicitly on `MatchSetupScreen` because cross-tab navigation doesn't create stack history, so no native caret appears automatically
- Swipe-to-delete kept but deferred for UI polish — the out-of-box behaviour doesn't match iOS platform feel and requires significant extra work to refine; will revisit when feature set is stable

## What I learned

React Navigation's back caret only appears when there's a previous screen in the current stack. Cross-tab `navigate()` doesn't reliably create that history, so screens navigated to cross-tab need an explicit `headerLeft` if a back affordance is required.

`navigate('ScreenName')` in a stack can push a new instance rather than popping to an existing one — always prefer `popToTop()` or `goBack()` when the intent is to go back, not forward.

## Problems / friction

Swipe-to-delete UX in React Native is not a platform primitive. Getting it to feel native (physics, haptics, animation curve) requires significant additional work beyond what `react-native-gesture-handler`'s `Swipeable` provides out of the box.

## Next step

UI consideration: replace swipe-to-delete with an explicit delete button inside detail screens, then revisit gesture-based deletion as a polish pass once the feature set is stable.
